{
  "name": "nestjs-remote-firebase-config",
  "version": "0.0.4",
  "description": "Config module which uses FirebaseFirestone as a config store",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && tsc",
    "prepublish": "npm run build"
  },
  "keywords": ["nestjs config", "firestore", "remote config", "shared config", "config service", "daymee opensourse"],
  "author": "Vladislav Len <heavenly.beta@gmail.com>",
  "license": "ISC",
  "repository": "https://github.com/daymee/nestjs-remote-config",
  "dependencies": {
    "@google-cloud/firestore": "^4.2.0",
    "@nestjs/common": "^6.7.0",
    "@types/node": "^14.11.2",
    "reflect-metadata": "^0.1.13",
    "rimraf": "^3.0.2",
    "rxjs": "^6.6.3",
    "typescript": "^4.0.3"
  }
}
